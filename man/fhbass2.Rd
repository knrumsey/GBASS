% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fhbass2.R
\name{fhbass2}
\alias{fhbass2}
\title{Fay-Herriot BMARS (fhbass2)}
\usage{
fhbass2(
  X,
  y,
  d,
  maxInt = 3,
  maxBasis = 1000,
  npart = NULL,
  nmcmc = 10000,
  nburn = 9001,
  thin = 1,
  moveProbs = rep(1/3, 3),
  gamma_v_prior_scale = 100,
  gamma_w_prior_scale = NULL,
  fh_sample_var = NULL,
  gamma_v_init = NULL,
  gamma_w_init = NULL,
  do_mh_gamma = TRUE,
  a_tau = 1/2,
  b_tau = NULL,
  a_lambda = 1,
  b_lambda = 1,
  scale = 1,
  Iw0 = rep(1, maxInt),
  Zw0 = rep(1, ncol(X)),
  verbose = TRUE
)
}
\arguments{
\item{X}{an Nxp matrix of predictors scaled to (0,1) recommended.}

\item{y}{length-N response vector.}

\item{d}{length-N vector of sampling variances (delta_i).}

\item{maxInt}{maximum interaction degree for BMARS bases.}

\item{maxBasis}{maximum number of basis functions.}

\item{npart}{minimum number of nonzero entries in a candidate basis.}

\item{nmcmc}{number of MCMC iterations.}

\item{nburn}{burn-in iterations (<= nmcmc).}

\item{thin}{thinning interval.}

\item{moveProbs}{probabilities for birth/death/mutate moves.}

\item{gamma_v_prior_scale}{half-Cauchy prior scale for gamma_v (>0).}

\item{gamma_w_prior_scale}{half-Cauchy prior scale for gamma_w (>0). If NULL, computed from fh_sample_var.}

\item{fh_sample_var}{prior guess of "between-area" variance (FH sampling error variance proxy).}

\item{gamma_v_init}{initial value for gamma_v (defaults to gamma_v_prior_scale).}

\item{gamma_w_init}{initial value for gamma_w (defaults to gamma_w_prior_scale).}

\item{do_mh_gamma}{logical; if FALSE, gamma_v and gamma_w are held fixed at initial values.}

\item{a_tau, b_tau}{IG prior parameters for tau (penalty/coeff scale parameter).}

\item{a_lambda, b_lambda}{Gamma prior for lambda (basis count penalty).}

\item{scale}{fixed variance scaling (default 1).}

\item{Iw0}{nominal weights for interaction degree sampling.}

\item{Zw0}{nominal weights for variable sampling.}

\item{verbose}{print progress.}
}
\value{
an object of class "fhbass2" containing posterior draws and basis lookup.
}
\description{
A version of BMARS which implements a Fay-Herriot style model:
y_i = B(x_i) + sqrt(w * v_i) * eps_i,   eps_i ~ N(0,1)
with a GIG prior on v_i centered around supplied delta_i = d_i, and a GIG prior on w.
Includes optional Metropolis updates for gamma_v and gamma_w with half-Cauchy priors.
}
\details{
IMPORTANT DESIGN CHOICES (vs your draft):
\itemize{
\item Removes the "beta * v * sqrt(w)" mean term entirely (beta fixed to 0).
\item Fixes scoping bugs in MH log-densities (closures inside fhbass2).
\item Fixes predict method dispatch + indexing, and adds posterior predictive option.
\item Separates "prior scale" from "initial value" for gamma parameters.
}
}
