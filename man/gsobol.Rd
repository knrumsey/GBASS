% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sobol.R
\name{gsobol}
\alias{gsobol}
\title{Sobol decomposition for GBASS models}
\usage{
gsobol(object, ...)
}
\arguments{
\item{object}{A fitted object of class \code{"gbass"}.}

\item{...}{Additional arguments passed to \code{BASS::sobol()}.}
}
\value{
An object of class \code{"bassSob"} returned by \code{BASS::sobol()}.
}
\description{
Computes Sobol sensitivity indices for a fitted \code{gbass} model by first
converting it to a compatible \code{BASS} object with \code{gbass2bass()},
and then calling \code{BASS::sobol()}.
}
\details{
This is a thin wrapper around \code{BASS::sobol()} for GBASS models.
Users who want direct access to the converted \code{BASS} object can call
\code{gbass2bass()} explicitly and then use \code{BASS::sobol()} themselves.
}
\examples{
n <- 100
p <- 4
X <- matrix(runif(n * p), nrow = n)
y <- apply(X, 1, ff1)

mod <- gbass(X, y, nmcmc = 1000, nburn = 900)

# Direct wrapper
sob <- gsobol(mod)

# Equivalent manual conversion
bm <- gbass2bass(mod)
sob2 <- BASS::sobol(bm)

}
\seealso{
\code{\link{gbass2bass}}, \code{\link[BASS]{sobol}}
}
